import { TypeSystemPolicy } from '@sinclair/typebox/build/esm/system/policy.mjs';
import { Immutable } from '@sinclair/typebox/build/esm/type/create/immutable.mjs';
import { Clone } from '@sinclair/typebox/build/esm/type/clone/value.mjs';
/** Creates TypeBox schematics using the configured InstanceMode */
export function CreateType(schema, options) {
    const result = options !== undefined ? { ...options, ...schema } : schema;
    switch (TypeSystemPolicy.InstanceMode) {
        case 'freeze':
            return Immutable(result);
        case 'clone':
            return Clone(result);
        default:
            return result;
    }
}
