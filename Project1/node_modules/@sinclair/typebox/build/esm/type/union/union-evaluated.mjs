import { CreateType } from '@sinclair/typebox/build/esm/type/create/type.mjs';
import { OptionalKind } from '@sinclair/typebox/build/esm/type/symbols/index.mjs';
import { Discard } from '@sinclair/typebox/build/esm/type/discard/index.mjs';
import { Never } from '@sinclair/typebox/build/esm/type/never/index.mjs';
import { Optional } from '@sinclair/typebox/build/esm/type/optional/index.mjs';
import { UnionCreate } from '@sinclair/typebox/build/esm/type/union/union-create.mjs';
// ------------------------------------------------------------------
// TypeGuard
// ------------------------------------------------------------------
import { IsOptional } from '@sinclair/typebox/build/esm/type/guard/kind.mjs';
// prettier-ignore
function IsUnionOptional(types) {
    return types.some(type => IsOptional(type));
}
// prettier-ignore
function RemoveOptionalFromRest(types) {
    return types.map(left => IsOptional(left) ? RemoveOptionalFromType(left) : left);
}
// prettier-ignore
function RemoveOptionalFromType(T) {
    return (Discard(T, [OptionalKind]));
}
// prettier-ignore
function ResolveUnion(types, options) {
    const isOptional = IsUnionOptional(types);
    return (isOptional
        ? Optional(UnionCreate(RemoveOptionalFromRest(types), options))
        : UnionCreate(RemoveOptionalFromRest(types), options));
}
/** `[Json]` Creates an evaluated Union type */
export function UnionEvaluated(T, options) {
    // prettier-ignore
    return (T.length === 1 ? CreateType(T[0], options) :
        T.length === 0 ? Never(options) :
            ResolveUnion(T, options));
}
