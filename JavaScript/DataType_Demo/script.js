
/*
console.log('practice');
    variables
    var = probably not use ever
    let = scope based
    const = CONSTANT
 *!/
{
    let dog = "woof"
    {
       let dog = "meow"

    }
    console.log(dog)
}
console.log(typeof dog) //this func tells the computer to display the type of dog

Numbers
 let age = "doggy"

let input = "24"
console.log(input)//(user input will always be a string! call for "")
console.log(typeof input)
let parsedNumber = Number(input)
console.log(parsedNumber)
let secondInput = Number(newInput)
console.log(typeof parsedNumber)
let parsedeIntNewInput = parseInt(newInput)
console.log(parsedeIntNewInput)

let test= false;
    console.log(test)

falsey and truthy values
falsey = 0, "", false, undefined, null

Truthy
1, " ", if it exists

Stricky quality and loosy quality
loose equality is (==)
strict equality is (===)

does not equal (!==)
*/
//Boolean use ?/: to compare results
//let result = a > 21 ? "great job" : "bad job"
// "?" is used as true, ":" is used as false